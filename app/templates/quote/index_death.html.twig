{% extends 'base.html.twig' %}

{% block title %}Quotes{% endblock %}

{% block body %}
    <style>
        .wrapper {
            margin: 1em auto;
            width: 95%;
        }
    </style>

    <div class="wrapper">
        <h1>Great DeathNote</h1>
        <table class="table table-striped table-hover table-bordered">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Born Year</th>
                <th scope="col">City Of Born</th>
                <th scope="col">Dead Year</th>
                <th scope="col">City of Dead</th>
            </tr>
            </thead>
            <tbody>
            {% for person in persons %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td>{{ person.name}}</td>
                    <td>{{ person.born_year}}</td>
                    <td>{{ person.city_of_born}}</td>
                    <td>{{ person.dead_year}}</td>
                    <td>{{ person.city_of_dead}}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

